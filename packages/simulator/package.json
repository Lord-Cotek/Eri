{
  "name": "@eri/simulator",
  "version": "0.1.0",
  "private": true,
  "description": "Ẹ̀rí simulated sentinel — a first-class client of the real wire protocol.",
  "type": "module",
  "bin": {
    "eri-sim": "./dist/cli.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "sim": "node dist/cli.js",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@eri/protocol": "*"
  },
  "devDependencies": {
    "@types/node": "^20.17.6",
    "typescript": "^5.6.3"
  }
}
